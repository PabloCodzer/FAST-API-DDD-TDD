# Instalar e iniciar a venv
    mkdir meu_projeto_fastapi
    cd meu_projeto_fastapi
    python -m venv venv
    source venv/bin/activate  # Ou `venv\Scripts\activate` no Windows
    pip install fastapi uvicorn
    touch main.py

# permissoes
    Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

#instalar uvicorn
    pip install uvicorn

# Rodar main
    uvicorn main:app --reload

# rodar em produçao
    uvicorn main:app --host 0.0.0.0 --port 8000

# Criar arquivo requirements
    pip freeze > requirements.txt


 #################################
 # rdar dentro de um docker
 # colar dentro de um dockerfile 
 #################################
 # Usar uma imagem base do Python
    FROM python:3.11

    # Instalar as dependências
    WORKDIR /app
    COPY requirements.txt .
    RUN pip install -r requirements.txt

    # Copiar o código da aplicação
    COPY . .

    # Expor a porta da aplicação
    EXPOSE 8000

    # Comando para rodar a aplicação usando Gunicorn e Uvicorn Worker
    CMD ["gunicorn", "main:app", "-w", "4", "-k", "uvicorn.workers.UvicornWorker"]

# rodar o container
    docker build -t my-fastapi-app .
    docker run -p 8000:8000 my-fastapi-app